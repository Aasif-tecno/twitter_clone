<%= form_with(model: tweeet) do |form| %>
  <% if tweeet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tweeet.errors.count, "error") %> prohibited this tweeet from being saved:</h2>

      <ul>
        <% tweeet.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :tweeet %>
    <%= form.rich_text_area :tweeet, class: "form-control"%>
  </div>
   <div class="form-group">
      <%= form.file_field :tweeet_image, class: "form-control-file"%>
    </div>

  <div class="actions">
    <%= form.submit "Tweet", class: "btn btn-primary" %>
  </div>
<% end %>
